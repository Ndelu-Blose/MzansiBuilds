# Copy to `frontend/.env.local` for local development (do not commit real secrets).
#
# Do not add RESEND_API_KEY, Supabase service_role, or database credentials here —
# only `REACT_APP_*` client-safe values. Server secrets belong in `backend/.env`.

# Supabase — Project Settings → API (Project URL + anon public key)
REACT_APP_SUPABASE_URL=https://your-project-ref.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key

# GitHub OAuth: paste into GitHub → OAuth App → Authorization callback URL.
# Usually https://YOUR_PROJECT_REF.supabase.co/auth/v1/callback (Supabase → Auth → Providers → GitHub).
# Optional: omit if it matches REACT_APP_SUPABASE_URL + /auth/v1/callback
# REACT_APP_GITHUB_OAUTH_CALLBACK_URL=https://your-project-ref.supabase.co/auth/v1/callback

# Google Cloud → OAuth 2.0 Client → Authorized redirect URIs (same Supabase callback as GitHub).
# Optional: omit if it matches REACT_APP_SUPABASE_URL + /auth/v1/callback
# REACT_APP_GOOGLE_OAUTH_CALLBACK_URL=https://your-project-ref.supabase.co/auth/v1/callback

# FastAPI backend base URL (no trailing slash)
REACT_APP_BACKEND_URL=http://localhost:8000

# Optional: when the dev server runs behind HTTPS or a reverse proxy, CRA may need this for HMR WebSockets.
# Example: WDS_SOCKET_PORT=443
# WDS_SOCKET_PORT=

# Google/GitHub login: enable providers in Supabase and set OAuth clients in Google Cloud + GitHub.
# Supabase → Authentication → URL configuration: add Redirect URLs for your dev/prod origin:
#   /auth/callback (OAuth), /auth/confirmed (email signup confirm), /auth/reset-password (password reset).
# See README.md → "OAuth: Google and GitHub" and root README → Deploy frontend (Vercel).

# --- Vercel (Production) ---
# In Vercel → Project → Settings → Environment Variables, add the same REACT_APP_* keys for Production (and Preview if needed).
# Use your production site URL in Supabase: Authentication → URL configuration → Site URL and Redirect URLs
# (e.g. https://your-app.vercel.app and https://your-app.vercel.app/auth/callback).
# REACT_APP_BACKEND_URL must be your deployed API origin (not localhost) if the app calls the FastAPI backend.
